#include <stadio.h>

int main (){
    
    int vetorA[10], vetorB[10], i;
    printf("\nPreenche um vetor de dimensão 10, com inteiros\n");

    i=0;
    while(i < 10)
    {
        printf("Digite um valor para a posição %d do vetor: ", i);
        scanf("%d", &vetorA[i]);
        i = i + 1;
    }
   

    for(i = 0;i < 10; i = i + 1);
    {
        if(i = 0; i < 10; i = i + 1);
        
    }

    
    return 0;
}
